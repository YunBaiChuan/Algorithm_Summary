# 09月08日

## 第一题：小红的陡峭值 https://www.nowcoder.com/practice/e723c5acb14f4e26931135fe033fe65e?channelPut=tracker2

**思路：数学。主要是推式子算数学期望，即平均绝对差的两倍**

**Cpp代码：**
```cpp
#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

using ll = long long;

const int MOD = 1e9 + 7;

ll Pow(ll a, int b) {
    ll res = 1;
    while (b) {
        if (b & 1) {
            res = res * a % MOD;
        }
        a = a * a % MOD;
        b >>= 1;
    }
    return res;
}

ll Inv(ll a) {
    return Pow(a, MOD - 2);
}

int n;
vector<int> a;

void Solve() {
    cin >> n;
    a.resize(n);
    for (auto& x : a) {
        cin >> x;
    }
    sort(a.begin(), a.end());
    ll sum = 0;
    for (int i = 0; i < n; i++) {
        sum = (sum + (ll)a[i] * (i - (n - 1 - i))) % MOD;
    }
    cout << sum * 2 * Inv(n) % MOD;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    Solve();
}

```