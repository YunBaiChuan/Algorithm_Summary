# 09月25日

## 第一题：三角形取数 https://www.nowcoder.com/practice/ceea5825472940dabfec917ef93538e6?channelPut=tracker2

**思路：网格图dp。加以限制的网格图dp，转移方向有3个，然后转移次数有k加以限制**

**Python代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''`

'''

def solve():
    n, k = MII()
    a = [LII() for _ in range(n)]

    if n == 1:
        print(a[0][0])
        return

    a[n - 1] = [
        -inf if abs(i - (n - 1)) > k else value
        for i, value in enumerate(a[n - 1])
    ]

    for i in range(n - 2, -1, -1):
        for j in range(len(a[i])):
            a[i][j] += max(a[i + 1][j: j + 3])

    print(a[0][0])

t = 1
# t = II()
for _ in range(t):
    solve()
```