# 07月26日

## 第一题：2^20 https://www.nowcoder.com/practice/c44464a6d8dd42d0bb09c76800d20116?channelPut=tracker2

**思路：思维 + 位运算。考虑题目给定的两种操作：1.直接+1；2.不断*2，其实就是左移一位。题目要求通过尽量少的这两种操作，使得最终得到的数能够被$2^{20}$整除。这里其实要知道一个结论，对于能够被$2^x$整除的数来说，他的末尾x位都为0。以能够被$2^2$整除为例，那么满足这个条件的数，末尾两位应该都为0才行。所以说本题就是要让后20位都为0，并且尽量要让操作少，那就应该先进行+1操作，然后最多进行20次左移操作。不断算操作中的最小值，得到最终答案**

**代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''

'''

init = 1 << 20

def solve():
    n = II()
    ans = 20 # 最差情况为乘20次直接得到2^20
    for i in range(20): # 枚举优化，最多操作都是20了，i操作肯定 < 20
        tmp = n + i # 先加再乘，优先保证右侧零尽可能多，则操作会尽可能少
        j = 0
        while j < 20:
            if (tmp << j) % init == 0:
                ans = fmin(ans, i + j)
            j += 1
    print(ans)

# t = 1
t = II()
for _ in range(t):
    solve()
```