# 07月10日

## 第一题：小红的正整数构造 https://www.nowcoder.com/practice/7aa37cbc28034fe5af562ec7e44d1e76?channelPut=tracker2

**思路：模拟。根据题意进行模拟即可，可以用l，r遍历找x倍数，也可以用找x倍数，然后判断是否在l，r之间。一般是用后者，对于l，r范围很大的数据来说比较有效**

**代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''

'''

def solve():
    l, r, x = LII()
    for y in range(x, r + 1, x):
        if l <= y <= r:
            print(y)
            return
    print(-1)

t = 1
# t = II()
for _ in range(t):
    solve()
```