# 07月07日

## 第一题：小A的位运算 https://www.nowcoder.com/practice/58fa8c46e9bc4bb299943cbae6054234?channelPut=tracker2

**思路：前后缀分解 + 奇妙读取。首先要使得n - 1个数的或最大，比较容易想到的就是先算出整个数组的或，之后再去除掉某个或，然后不断取最大值就是答案。但这有个问题，如何去掉或值中的某个元素影响？用异或吗？不对，以5 | 7 = 7来说，7 ^ 5 = 2，不具有对称性**

**那么此时就应该考虑前后缀分解了，先算出前后缀的或数组，接着就枚举不要某个元素，把剩下的前后缀或元素给或起来，就可以得到此时n - 1个数的或值了。最终，不断取fmax得到答案**

**注意：这题读取有点问题，他题目描述的是一行，但实际上分了多行，因此要用一个while判断是否把数据读够了，不够的话还得继续读，然后拼上去。当然用一次全部读取，然后再截取出来也行**

**代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''

'''

def solve():
    n = II()
    a = LII()

    while len(a) < n:
        a.extend(LII())

    pre = [0] * (n + 1)
    for i, x in enumerate(a):
        pre[i + 1] = pre[i] | x

    suf = [0] * (n + 1)
    for i in range(n - 1, -1, -1):
        suf[i] = suf[i + 1] | a[i]

    ans = -inf
    for i in range(n):
        ans = fmax(ans, pre[i] | suf[i + 1])
    print(ans)

t = 1
# t = II()
for _ in range(t):
    solve()
```