# 07月08日

## 第一题：连接非零数字并乘以其数字和 II https://leetcode.cn/problems/concatenate-non-zero-digits-and-multiply-by-sum-ii/description/?envType=daily-question&envId=2026-07-08

**思路：数位前缀和。非常巧妙的思路，用的是数位前缀和，由于数位可能存在0，因此还要记录一个非零数个数前缀和数组。可以发现预处理的前缀和数组，有以下递推公式$pre\_num[r] - pre\_num[l] * pow10[length]$，最终就可以实现区间内的ans查询了**

**代码：**
```python
MOD = 10 ** 9 + 7
MAX_N = 10 ** 5 + 1

pow10 = [1] * MAX_N
for i in range(1, MAX_N):
    pow10[i] = (pow10[i - 1] * 10) % MOD

class Solution:
    def sumAndMultiply(self, s: str, queries: List[List[int]]) -> List[int]:
        n = len(s)
        sum_d = [0] * (n + 1)
        pre_num = [0] * (n + 1)
        sum_non_zero = [0] * (n + 1)
        for i, d in enumerate(map(int, s)):
            sum_d[i + 1] = sum_d[i] + d
            pre_num[i + 1] = (pre_num[i] * 10 + d) % MOD if d else pre_num[i]
            sum_non_zero[i + 1] = sum_non_zero[i] + (d > 0)

        ans = []
        for l, r in queries:
            r += 1
            length = sum_non_zero[r] - sum_non_zero[l]
            x = pre_num[r] - pre_num[l] * pow10[length]
            ans.append(x * (sum_d[r] - sum_d[l]) % MOD)
        return ans
```

## 第二题：或与异或 https://www.nowcoder.com/practice/a4bddff248c1424c90ee6cf309cf303f?channelPut=tracker2

**思路：位运算 + 思维。根据或、与、异或这三个操作，虽然可以进行无限次操作，但是结果是有限的。因此，我们可以算出一个ans集合，然后判断target是否在ans集合中即可。这里面有个恒等式为(a & b) ^ b = ~a & b需要记一下**

**代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''

'''

def solve():
    a, b, target = LII()
    ans = {0, a, b, a | b, a & b, a ^ b, a & ~b, ~a & b}
    print("YES" if target in ans else "NO")

# t = 1
t = II()
for _ in range(t):
    solve()
```