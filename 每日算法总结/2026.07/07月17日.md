# 07月17日

## 第一题：Forsaken喜欢正方形 https://www.nowcoder.com/practice/5dd2716669a94a6d9ea419518b0f8c51?channelPut=tracker2

**思路：模拟。主要是正方形怎么判断？可以这样判断：首先计算出4个点，也就是6条边的长度（这里直接算平方会更方便），然后从小到大排序；如果满足以下条件：1.dist[0] > 0；2.前四条边相同；3.后两条边相同；4.前面边的2倍，是否与后面边相同，说明就是正方形；之后就可以枚举每个点移动四个方向，然后用这个正方形判断逻辑修改一个点是否满足构成正方形；最终说明都不满足，直接返回相应结果即可**

**代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmin = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''

'''

def is_square(points):
    dists = []
    for i in range(4):
        for j in range(i + 1, 4):
            x1, y1 = points[i]
            x2, y2 = points[j]
            dist = (x1 - x2) ** 2 + (y1 - y2) ** 2
            dists.append(dist)

    dists.sort()
    return (dists[0] > 0 and
            dists[0] == dists[1] == dists[2] == dists[3] and
            dists[4] == dists[5] and
            dists[4] == 2 * dists[0])

def solve():
    points = [tuple(MII()) for _ in range(4)]

    if is_square(points):
        print("wen")
        return

    directions = [(1, 0), (-1, 0), (0, 1), (0, -1)]
    for i in range(4):
        x, y = points[i]
        for dx, dy in directions:
            new_point = (x + dx, y + dy)
            new_points = points[:i] + [new_point] + points[i + 1:]
            if is_square(new_points):
                print("hai xing")
                return

    print("wo jue de bu xing")

t = 1
# t = II()
for _ in range(t):
    solve()
```